/**
 * Version-scoped presentation flags.
 * Daily Challenge remains implemented; Version 1.0 only hides discovery UI.
 */
export const SHOW_DAILY_CHALLENGE = false;

/** Church Mode shared-screen V1 is part of the Version 1.0 release. */
export const SHOW_CHURCH_MODE = true;

/** Unreleased discovery surfaces remain in source but are hidden for Version 1.0. */
export const SHOW_FUTURE_FEATURES = false;
